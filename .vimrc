" Set compatibility to Vim only
set nocompatible

" Automaticlly wrap text that exteds beyond the screen length
set wrap

" Encoding
set encoding=utf-8

" Set hybrid line numbers
set number relativenumber

" Status bar
set laststatus=2

" Turn off windows bell sound
set belloff=all